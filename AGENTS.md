## Project Rules

- This repository is a static personal site. Keep the entry page in `index.html`.
- Put image and media assets in `assets/`. Keep inline CSS and JS in `index.html` unless a feature becomes large enough to justify extraction.
- Make surgical edits: preserve existing content, links, and section structure unless explicitly requested.
- Verify visual changes by opening the static page in a browser or local HTTP server.
